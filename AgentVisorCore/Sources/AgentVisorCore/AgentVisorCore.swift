// Pure logic shared between the AgentVisor macOS app and its tests.
// Modules here have no AppKit/SwiftUI dependencies so they can be tested
// via `swift test` without an Xcode test bundle.
public enum AgentVisorCore {}
