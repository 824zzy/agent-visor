import XCTest
@testable import AgentVisorCore

final class SmokeTests: XCTestCase {
    func testPackageBuilds() {
        // Phase 0 sanity: confirms the test target links and runs.
        XCTAssertTrue(true)
    }
}
